// Membuat string
string nama = "Ilham";

// Melakukan perulangan untuk setiap karakter
for (int i = 0; i < nama.Length; i++)
{
    // Menampilkan karakter satu per satu
    Console.WriteLine("Index " + i + ": " + nama[i]);
}